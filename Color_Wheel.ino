const int wheel[] = {
  0xFF, 0x00, 0x00, // red
  0xFF, 0x2A, 0x00, // 
  0xFF, 0x55, 0x00, // 
  0xFF, 0x7F, 0x00, // orange
  0xFF, 0xAA, 0x00, //
  0xFF, 0xD4, 0x00, // 
  0xFF, 0xFF, 0x00, // yellow
  0xAA, 0xFF, 0x00, // 
  0x55, 0xFF, 0x00, //
  0x00, 0xFF, 0x00, // green
  0x00, 0xFF, 0x55, //    green
  0x00, 0xFF, 0xFF, // 
  0x00, 0x80, 0xFF, //    blue
  0x00, 0x00, 0xC0, // blue
  0xAA, 0x00, 0xFF, //    blue
  0xFF, 0x00, 0xAA, // 
};

void colorWheel (int offset, Adafruit_NeoPixel circle) {
  int p;
  for (int i = 0; i < (ledNumber * 3); i += 3) {
    p = (i/3) + offset;
    p = p % 16;
    int col = circle.Color(wheel[i], wheel[i + 1], wheel[i + 2]);
    circle.setPixelColor(p, wheel[i], wheel[i + 1], wheel[i + 2]);
    //    circle.setPixelColor(i / 3, col);
    Serial.print(String(i / 3) + ": ");
    Serial.println(String(i));
  }
  circle.show();
}
